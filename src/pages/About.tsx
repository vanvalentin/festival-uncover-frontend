import React, { ReactNode } from "react";
import Navbar from "../components/Navbar";

// require('../styles/about.scss');

export default class About extends React.Component<object, object> {

    constructor(props: object) {
        super(props);
    }

    componentDidMount(){
    }

    render() {
        return (
            <div className="about">
            </div>
        );
    }
}