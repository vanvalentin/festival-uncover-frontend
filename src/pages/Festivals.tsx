import React, { ReactNode } from "react";
import Navbar from "../components/Navbar";

// require('../styles/festivals.scss');

export default class Festivals extends React.Component<object, object> {

    constructor(props: object) {
        super(props);
    }

    componentDidMount(){
    }

    render() {
        return (
            <div className="festivals">
            </div>
        );
    }
}